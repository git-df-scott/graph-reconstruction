# Handoff: GRC counterexample campaign (2026-09-02, 03:55 UTC)

> **Superseded by `ASTRA_HANDOFF.md`.**  This snapshot predates a divergent
> continuation, the measured failure of the order-8 runtime estimate, and the
> 2026-09-04 hostile verifier audit.  Do not use its running-process state as
> current campaign canon.

Branch `claude/grc-counterexample-35nwgv`, draft PR #1
(https://github.com/git-df-scott/graph-reconstruction/pull/1). Ledger:
`docs/PLAN_2026-09-01.md` (results log at the end; every entry tagged
`[LIT-READ]`, `[PROVED]`, or `[COMPUTED]`). Verdict: `GRC CE: NO`.

## Where the plan stands

Phase 1 (exhaustive small-order instruments): done. Named-colour census
zero-globalizer at orders 6-7 (order 8 rerunning after a container
restart); Kocay covering rank corank 1 at orders 5-8; blow-up deck
searches null through quotient order 8; vector-deck theorem proved
(twin classes all of size >= 3 are reconstructible).

Phase 2: done. Two-graph MaxSAT stops at order 9; STS(13)/STS(15)
incidence screen complete, no deck collision; Ivanov near-miss anatomy
superseded by the theorem.

Phase 3 (in progress): `scripts/deck_fixed_sat.py` fixes a candidate G
and searches all mates by SAT; it is the workhorse. Everything below is
UNSAT (reconstructible) unless stated:

- bi-circulants, all at orders 14, 16, 18; order 20 sampled, resumed at 2,750/12,433
- twin blow-ups (cells of size 1-2), all 28,948 at order 14 (quotient 7); 29,515 at order 16 (quotient 8 sample); order 14 quotient-8 sample resumed at 21,920/29,923
- automorphism cycle types at order 14: 2^7, 2^6 1^2, 3^4 1^2 complete (about 11-12k each); 3^4 2 resumed at 2,904, then six more types queued
- Hong regime (asymmetric, no controllable card): 39,762 at order 14; order 15 resumed at 7,896/13,691; order 16 not started
- switching-type pairs: three order-14 instances UNSAT
- Delta stratification (`scripts/delta_sat.py`): exact through order 10 (4- and 6-edge shapes), out of budget beyond
- complement-closed decks (`scripts/complement_deck_sat.py`): UNSAT at 5, 8, 13; order 12 and the first order-16 instance hit their caps

## Machine state after the restart

Scratchpad (survives restarts, not commits):
`/tmp/claude-0/-home-user-graph-reconstruction/a94ca821-c174-584e-8525-98c38bbff63a/scratchpad`
It holds the pynauty build, geng, python-sat/flint (`pylib`), the
`sigma_dfs8` binary, all feeds (`feed_*.g6`), all outputs (`verify_*.out`,
one line per graph), and the runner scripts. Environment for every script:

    S=/tmp/claude-0/-home-user-graph-reconstruction/a94ca821-c174-584e-8525-98c38bbff63a/scratchpad
    export PYTHONPATH=$S/pynauty-2.8.8.1/build/lib.linux-x86_64-cpython-311:$S/pylib
    GENG=$S/pynauty-2.8.8.1/src/nauty2_8_8/geng

If the scratchpad is gone: rebuild pynauty 2.8.8.1 from sdist
(`make pynauty && make nauty-programs`), `pip install --target $S/pylib
python-sat python-flint`, `gcc -O2 -DMAXN=8 -DMAXE=28 -o $S/sigma_dfs8
scripts/sigma_dfs.c`, then regenerate feeds with `scripts/candidate_feed.py`.

Running now (launched 03:52 UTC by `$S/resume_after_restart.sh`, log
`$S/resume.log`): the four resumed sweeps above, plus `sigma_dfs8 8`
restarted from scratch (expect 5+ hours; prints only at the end to
`$S/sigma_dfs8.out`). An hourly keepalive routine
(`trig_01HWyugE38poxt3wG937saGz`, fires at :22) wakes the session.

## How to carry on (copy-paste)

1. Check state:

       cd $S; for f in verify_bu14_q8 verify20 verify_cardsym_15 verify_aut_3,3,3,3,2; do echo "$f $(wc -l < $f.out)"; done; cat verify*.out | grep -vc UNSAT; cat sigma_dfs8.out; tail -3 aut14.out

   Any line in a `verify_*.out` that is not `UNSAT` is a hit: replay it
   with `python3 scripts/deck_fixed_sat.py '<graph6>' --verbose`; a true
   counterexample prints `'SAT'` with `verified_counterexample: True`.

2. Resume any sweep that died (output is one line per input line):

       resume() { done=$(wc -l < "$2"); tail -n +$((done+1)) "$1" | python3 scripts/deck_fixed_sat.py - >> "$2" 2>&1; }
       resume $S/feed_bu14_q8.g6 $S/verify_bu14_q8.out

3. Record each finished sweep in `docs/PLAN_2026-09-01.md` above the
   final `**Verdict at this point.**` block, commit with the required
   trailer, push, and refresh the PR #1 body if instruments changed.

4. Next queued work, in order of value:
   - Hong regime at order 16: `python3 scripts/card_symmetry_climb.py --n 16 --restarts 40 --steps 2500 --objective hong --t0 2 --t1 0.2 > $S/feed_cardsym_16.g6`, then verify.
   - Blow-ups at order 15-16 with mixed cells: `candidate_feed.py --blowup 9 --n 15 --sample 30000 --geng $GENG` (and `--blowup 8 --n 15`).
   - Remaining order-14 cycle types not yet run: 4,4,4,2; 6,6,2; 5,5,2,2; 3,3,3,1,1,1,1,1.
   - Complement-closed decks at 16/17 need a better encoding (the CEGAR over self-complementary solutions is the bottleneck); skip unless reworked.
   - Delta stratification beyond order 10 needs degree-pinned instances (`--degrees`) to be tractable.

5. Ideas not yet built: `twocard_filter.py` in front of a large
   semi-structured random sweep at 14-16 (it is 30x faster than the
   verifier); a three-orbit circulant feed at orders 15 and 18; the
   pseudosimilar census at orders 8-9 (`scripts/pseudosimilar_census.py`).

## Do not repeat

- Do not `pkill -f <pattern>` from a command line that itself contains the pattern; it kills the shell (happened three times). Kill by PID or use the `[x]yz` bracket trick.
- The two-graph encoders (`hypomorphism_sat.py`, `delta_sat.py`) do not scale past order 10-12; use the deck-fixed verifier.
- The `f(G)` hill-climb (`deck_climb.py`) is too slow at order 14; the Hong-objective generator replaced it.
